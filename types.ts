
export enum Step {
  PROPOSAL = 'PROPOSAL',
  SURPRISE = 'SURPRISE',
  DATE_PLANNER = 'DATE_PLANNER',
  FINAL = 'FINAL'
}

export interface DateOption {
  id: string;
  label: string;
  icon: string;
}
